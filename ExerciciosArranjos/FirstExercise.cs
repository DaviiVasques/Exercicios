using System;

namespace ExercicioOne
{
    class FirstExercise
    {
            /*Console.Write("Digite a quantidade de pessoas a serem regustradas: ");
            int n = int.Parse(Console.ReadLine() ?? "0");
            int j = 1;
            double[] vect = new double[n];

            for (int i = 0; i < n; i++)
            {
                System.Console.Write("Digite a " + j++ + "º altura: " );
                vect[i] = double.Parse(Console.ReadLine() ?? "0", CultureInfo.InvariantCulture);


            }

            double soma = 0.0;

            for (int i = 0; i < n; i++)
            {
                soma += vect[i]; 
            }

            double avg = soma / n;

            Console.WriteLine($"O peso médio é de {avg.ToString("F2", CultureInfo.InvariantCulture)}");
            */
    }
}